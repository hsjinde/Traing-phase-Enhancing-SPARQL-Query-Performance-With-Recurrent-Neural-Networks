# Minimal runtime dependencies for the training-phase utilities in this repo.
# The original Conda/Spyder environment export contained local build paths,
# which are not portable across machines.
numpy>=1.18,<1.20
pandas>=1.1,<1.4
openpyxl>=3.0,<4
nltk>=3.6,<4
tensorflow==2.3.0
Keras==2.4.3
transformers==4.9.2
